<p style="color: green"><%= notice %></p>

<h1>Expenses</h1>

<div id="expenses">
  <% @expenses.each do |expense| %>
   <div id="<%= dom_id expense %>">
   <% if can? :destroy, expense %>
  <p class="container-flud mx-3 px-2 bg-white border-bottom">
    <strong>Name:</strong>
    <%= expense.name %>
  </p>

  <div class="container-flud mx-3 px-2 bg-white border-bottom">
    <p class="py-2">Expenseº<%= expense.created_at.to_time.to_i %> <br/>
    <span class="fa fa-calendar"></span> <span class="mx-1"><%= expense.created_at %></span>
    </p>
  </div>

  <p class="container-flud mx-3 px-2 bg-white border-bottom">
    <strong>Total Amount: $</strong>
    <%= expense.amount %>
  </p>

      <p>
        <%= link_to "Show this expense", expense_path(expense), class: "btn btn-primary"  %>
    </p> 
  <% end %>
</div>
  <% end %>
</div>