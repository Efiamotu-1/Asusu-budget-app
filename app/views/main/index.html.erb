<% if user_signed_in? %>
    <%= render partial: "shared/categories" %>
<% else %>
<div class="d-flex justify-content-center align-items-center" style="height: 90vh">
    <p class="text-gray fw-bold fst-italic fs-1">Asusu Budget App</p>
<div class="container d-flex flex-column justify-content-center gap-2 col-sm-4 position-absolute bottom-10" style="position: absolute; bottom: 20px">
               <button class="btn bg-blue"> <%= link_to 'Login', new_user_session_path, class: "nav-link"  %> </button>
               <button class="btn btn-secondary"> <%= link_to 'Sign Up', new_user_registration_path, class: "nav-link" %> </button>
</div>
</div>
<% end %>
